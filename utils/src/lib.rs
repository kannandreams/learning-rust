// Declare the modules to include them in the library crate
// by default, Rust uses lib.rs as the entry point for library crates, similar to how it uses main.rs for binary crates.
pub mod greet;
pub mod math;
